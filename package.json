{
  "name": "timestamp-microservice",
  "version": "1.0.0",
  "description": "An API that returns a timestamp both in unix and utc in JSON format.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Mediat Yusuff",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
